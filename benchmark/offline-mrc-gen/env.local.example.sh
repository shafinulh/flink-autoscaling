#!/usr/bin/env bash

# Set this to the local block_cache_trace_analyzer binary.
BLOCK_CACHE_TRACE_ANALYZER="/path/to/frocksdb/block_cache_trace_analyzer"

# Override these only if your repo layout differs from the default.
# ONLINE_MRC_DIR="${REPO_ROOT}/online_mrc"
# ONLINE_MRC_BUILD_DIR="${ONLINE_MRC_DIR}/build"
